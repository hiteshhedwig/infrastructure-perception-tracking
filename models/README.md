# Models

Large checkpoints are not committed.

Expected local checkpoint path for current demo reproduction:

```text
external/coopdet3d/weights/coopdet3d_vi_cl_pointpillars512_2xtestgrid_yolos_transfer_learning_best.pth
```

Use GitHub Releases, Git LFS, Hugging Face, Google Drive, or another host for heavy checkpoints.
