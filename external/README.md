# External dependencies

This folder is for Git submodules.

Main submodule:

```text
external/coopdet3d
https://github.com/hiteshhedwig/coopdet3d.git
branch: ipt-local-mods
```

Add it with:

```bash
bash scripts/setup/add_coopdet3d_submodule.sh
```
